# モンスター命中率異常 修正報告

## 原因

モンスターマスターの `hit` に二つの表現が混在していました。

- `0`: 基準命中100%を示す旧来の省略値
- `100以上`: 絶対値としての命中率
- 一部のRank1～85雑魚だけ `1～6`: 基準100への加算値

戦闘生成処理は非ゼロ値を絶対値として扱っていたため、`6` が `106%` ではなく `6%` として処理されていました。追憶の魔境・深層強化生成でも、この低い値を基準にランダム加算する経路がありました。

## マスター修正

| ID | モンスター | 修正前 | 修正後 |
|---:|---|---:|---:|
| 52 | シェイドクローラー | 1 | 101 |
| 155 | ソーンボア | 1 | 101 |
| 510 | シャードヘア | 5 | 105 |
| 556 | レイザーウイング | 6 | 106 |
| 605 | 聖域のセンチネル | 3 | 103 |
| 656 | スパークハウンド | 3 | 103 |

## 共通処理修正

- モンスターマスター読込時に命中値を絶対値へ統一
  - 未設定・0以下: 100
  - 1～49: 100を加算
  - 50以上: そのまま使用
- 通常戦闘の敵生成でも同じ正規化処理を使用
- 深淵・追憶の魔境の強化生成でも同じ正規化処理を使用
- 戦闘途中の旧セーブに `hit: 1～49` が残っていても、再開時に補正
- モンスターエディタの新規作成初期値を `hit: 100` へ変更
- エディタの表示を `hit（100=標準）` に変更
- Service Workerのキャッシュ世代を更新

## 点検結果

実行時の全モンスター348体を読み込み、雑魚区分254体について命中値を確認しました。正規化後に100未満となる雑魚モンスターは0体です。

ブラウザでの通しプレイは実施していません。JavaScript構文、マスター読込結果、旧値補正経路を確認しています。
