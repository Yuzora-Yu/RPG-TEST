# Abyss Outer Rim v003

- The former greenish inner floor is replaced by a neutral charcoal ruined-stone family.
- The decorative broken arch behind the altar is no longer placed.
- The abyss is a nine-cell floor-layer composition (`7..9, 6..8`), not a tall actor-layer overlay.
- The same nine cells are blocked, while the action uses an `interactionArea` so every outer perimeter cell can expose the entrance action.

Build with `tools/assets/build-abyss-outer-v003-assets.py`.
