PRISMA ABYSS Phase2M 引継ぎ

正本順位:
1. 次チャットでのユーザー最新指示
2. Phase2M適用後の最新ソース
3. GitHub main
4. 各引継ぎ・実装資料

Phase2M完了内容:
- オートセーブは既存localStorage同期保存を維持。
- IndexedDBへ手動No.1～9を実装。
- タイトルに「オートセーブから」「つづきから」「はじめから」を分離。
- 設定を「設定」「セーブ」の2タブ化。
- セーブ一覧は左3行＋右パーティ顔4点。
- 手動ロード／バックアップ読込は警告後、オート枠へ安全に複製して再開。
- 既存.rpgsave出力・読込を維持。出力はオート枠だけ。10枠一括出力なし。
- stats.playTimeMsを追加。前面のゲーム本編だけ加算し、hhhh:mm:ss、表示上限9999:59:59。
- 冒険の記録とセーブ一覧へプレイ時間表示。
- 手動保存はチェックサム、書込後確認、旧枠ロールバックあり。
- IndexedDB不可時は手動枠だけ無効化し、オートと既存バックアップを残す。

参照:
- docs/DESIGN_TEN_SLOT_SAVE_AND_BACKUP_20260804.md
- docs/IMPLEMENTATION_PHASE2M_SAVE_SLOTS_PLAYTIME_20260804.md
- save_slots.js
- tools/test-phase2m-save-slots-playtime.js

テスト:
- Phase1～Phase2Mとvalidation計26件合格。
- 迷宮250階ストレス、到達不能0。
- シナリオ未参照28件は既存警告。
- 実行環境Chromiumが起動停止するため、実ブラウザ／実スマホは未確認。

次の優先作業:
1. PC Chrome/Edge、Android Chrome、iPhone Safari/PWAで10枠実機回帰。
2. No.1/9保存、上書き、再起動保持、手動ロード→オート複製、バックアップ読込を確認。
3. 主人公カスタム画像・通常仲間・仲間モンスターの4顔表示確認。
4. IndexedDB不可、容量不足、プライベートブラウズ時の案内確認。
5. 縦横画面とsafe-areaの表示確認。
6. その後、総合引継ぎのP1/P2未完項目へ戻る。

禁止:
- オート保存を非同期IndexedDBへ一括置換しない。
- 手動ロードでApp.dataへ直接代入したまま続行しない。
- バックアップ読込で手動1～9を消さない。
- 外部フォルダ権限を10枠の必須条件にしない。
- 10枠一括バックアップを勝手に追加しない。
- playTimeを旧startTimeから推測補完しない。
