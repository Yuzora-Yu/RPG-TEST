# RPG-TEST 修正・機能追加レポート

作成日: 2026-07-27  
作業元: 添付 `RPG.zip`  
アセット参照: GitHub `main` ブランチの既存 `assets/`  

## 納品内容

- 更新版プロジェクト一式（添付元と同様に `assets/` は含めない）
- 変更ファイルのみのZIP
- 本レポート

## 変更ファイル

- `main.js`
- `battle.js`
- `story_logic.js`
- `guild.js`
- `guild_quests.js`
- `dungeon.js`
- `items.js`
- `menus_items.js`
- `menus_party.js`

`assets/` の追加・変更は行っていません。

---

## ① 試験ボスのグラフィック描画エラー

- ギルド昇格試験用モンスターが持つ `imageId` を、戦闘用オブジェクト生成時・戦闘状態保存時・復元時にも維持するよう修正しました。
- モンスター定義IDと表示画像IDが異なる場合でも、既存の画像別名指定を優先して描画します。
- 対象の試験ボスは、GitHub側に存在する既存画像 `assets/monsters/monster_301010.png` を参照します。

## ② システム発言の会話枠を小型化

- システム発言時の会話枠を、最大3行を想定した高さに縮小しました。
- システム発言は高さ112px、キャラクター発言は従来の高さ148pxを維持します。
- 名前欄の有無による見た目の差を残し、キャラクター会話との区別を明確にしています。

## ③ スイッチ式接続マップの旧セーブ互換

- スイッチ起動状態だけを保存するのではなく、エリア読込時に現在のマップ定義から地形変更を再適用する共通処理を追加しました。
- 過去バージョンで起動済みのスイッチについても、現在の座標・タイル定義に従って橋や通路を復元します。
- 描画のたびに安全に再適用できる冪等処理とし、同じ変更が重複して蓄積しないようにしています。

## ④ ギルド受付の「出る」重複

- ギルド受付固有UIに重複して追加されていた「出る」を削除しました。
- 共通施設レイアウト側の「出る」だけを表示します。

## ⑤ レベル50到達時の成長倍率表記

- レベル50到達時の追加成長処理は維持し、戦闘ログに表示されていた成長倍率文だけを非表示にしました。
- 同系統のレベル100到達時も、倍率文を表示せず結果のみ反映する形に統一しました。

## ⑥ 会話後にクエスト受注選択を表示

- `startEventId` が設定されている依頼では、初回に開始会話を実行してから受注確認を表示します。
- 会話済み状態を依頼ごとに保存し、受注確認を閉じた場合に同じ開始会話を毎回繰り返さないようにしました。
- シナリオ正本に開始会話が存在しない依頼へ、新規の会話本文を自動生成する変更は行っていません。

## ⑦ 戦闘終了時の経験値配分

- 戦闘参加・生存: 100%
- 戦闘参加・戦闘不能: 50%
- 控えメンバー: 25%

追加対応:

- 戦闘不能者が経験値でレベルアップしても、勝利処理中に自動蘇生しないよう修正しました。
- 戦闘後HP/MP回復と特性成長は、従来どおり実際に戦闘して生存したメンバーだけを対象にします。
- 経験値減衰が発生した場合は、勝利ログへ簡潔な説明を表示します。

---

# ⑧ ギルドクエスト大規模変更

## 依頼迷宮の追加

- C級以上の依頼候補へ、SSR以上限定のランダムダンジョン挑戦依頼を追加しました。
- 既存の深淵系ランダムダンジョン生成・戦闘・階層移動を流用しています。
- 依頼内容は受注時に確定してセーブへ保存するため、再読込しても背景・階層数・敵・ボス・ギミックが変わりません。

依頼ごとにランダム決定する要素:

- 背景テーマ
- 属性テーマ
- 階層数
- 通常モンスター候補
- レアモンスター候補
- 最下層ボス
- 敵能力倍率
- 追加スキル・耐性・特性
- 特殊ギミック
- 報酬

## 属性テーマと敵加工

- 炎・氷・雷・風・光・闇を中心とした属性テーマを用意しました。
- 既存モンスターデータを複製してランタイム加工し、元データ自体は変更しません。
- 冒険者ランク、累積ギルド経験値、ストーリー進行、到達済み深淵階層、依頼レアリティを参照して能力を強化します。
- テーマ属性の攻撃倍率、耐性、追加スキル、再生、根性などを敵個体へ付与できます。
- 通常依頼敵も進行度に応じて緩やかに無上限成長します。

## EX依頼

- EX依頼では、稀に「レアモンスター遭遇率50%」の迷宮が生成されます。
- この恩恵が付く場合は、味方への常時猛毒、通常敵の大幅強化などの不利効果を併設します。
- 50%判定は遭遇全体に対して1回行い、レア候補数によって確率が過剰上昇しないようにしています。

## UR以上のボス戦限定依頼

- UR以上では、階層探索を行わずボス戦だけに挑む依頼が候補に入ります。
- ボス能力は冒険者ランク、累積ギルド経験値、ストーリー進行、到達階層、レアリティを参照し、固定上限を設けず上昇します。
- 強化量に応じて報酬量・報酬品質も上昇します。

## 報酬追加

- 依頼難度に応じた `RANK**` 武器 `+3` を報酬候補へ追加しました。
- EXの探索・ボス依頼では、交換所にないものを含む特性の書を抽選できます。
- 依頼迷宮は通常依頼よりギルド経験値・ギルドポイント・Gold・装備報酬を強化しています。

## 1日の受注回数

- 通常のギルドクエスト受注上限を、ローカル日付単位で1日10件にしました。
- 1日1回、既存のテスト用リワード広告処理を流用して受注可能数を10件追加できます。
- 通常分、広告追加分、広告利用済み状態をセーブへ保持します。
- 日付が変わると自動的に当日分へ更新します。

---

# ⑨ スキル書・仲間モンスター関連

## スキル書ドロップ

- 倒した雑魚・ボス個体ごとに0.5%でスキル書を抽選します。
- 対象スキルは、その個体が所持しているID100以上のスキルからランダムに選びます。
- 現在存在するID100以上のスキルについて、静的なアイテム定義を追加しました。

## 通常仲間の習得枠

- 通常仲間は、スキル書による追加習得を2枠まで保持します。
- 3個目を覚える場合は、既存のスキル書習得スキルから入れ替え対象を選択します。
- 職業・固有・レベル習得スキルとは別枠で管理します。

## 仲間モンスターの習得枠

- 仲間モンスターの所持スキル上限を合計8個にしました。
- 9個目を覚える場合は入れ替え対象を選択します。
- 仲間モンスターはレベルアップによる新規スキル習得を行いません。

## 戦闘後のスキル成長

- 戦闘後、編成中の仲間モンスターについて低確率でスキル成長候補を抽選します。
- 同属性・同種別で、現在より上位と判定できるスキルへ変化します。
- 変化前に確認ダイアログを表示し、「いいえ」でキャンセルできます。

## 転生の実

- 仲間モンスターを対象に選んだ場合、転生の実を使用できないようにしました。

## 合成の壺とモンスター合成の基盤

将来の「モンスター育成所」接続用として、以下の中核処理と保存項目を実装しました。

- ベースAの名前・見た目を維持
- AとBのスキルを統合し、最大8個を引き継ぐ
- レベル1へ戻す
- 初期ステータスをA+Bの各値の4分の1にする
- 素材Bを仲間一覧から除外
- 合成の壺を1個消費
- 合成前プレビューと、8個超過時の選択要求

また、次の入手・解放フックを追加しています。

- クリア後の通常ランダムダンジョンで、合成の壺をごく低確率ドロップ
- モンスター格闘場Sランク初回クリア通知用の処理
- Sランク初回クリア後、仲間モンスターのパーティ編成上限を1体から2体へ拡張
- Sランク初回報酬として合成の壺を付与できる処理

### 将来施設に関する範囲

現行プロジェクトには「モンスター育成所」「モンスター格闘場」の完成済み画面・イベント導線が存在しないため、今回は保存互換を含む中核API、アイテム、編成制限、報酬・解放フックまでを実装しています。施設画面が実装された際に、これらの処理を呼び出すことで接続できます。

---

## セーブ互換対応

- 新規保存項目が存在しない旧セーブでは、読込時に既定値を補完します。
- ギルド日次受注数、依頼迷宮設定、スキル書習得枠、仲間モンスター合成関連、格闘場解放状態を追加データとして保持します。
- 既存モンスター・スキル・装備の正本データを直接破壊せず、依頼戦闘用の加工値として保持します。
- 起動済みマップスイッチは、旧座標の保存データではなく現在のマップ定義から復元します。

## 確認条件

ユーザー指定に従い、リポジトリ付属の検証ツール、テストスクリプト、実ゲーム起動による動作検証は実行していません。変更箇所の参照関係、保存データ経路、ID・画像パス、呼び出し元と呼び出し先をコード上で読み合わせています。

## 推奨する手動確認箇所

1. 各ランク昇格試験ボスの画像と、戦闘再開後の画像
2. システム発言3行、キャラクター発言、長文時の表示
3. 起動済み橋・通路を含む旧セーブでのエリア再入場
4. ギルド受付の退出ボタン数
5. レベル49→50到達ログ
6. 開始会話付き仲間依頼の初回・キャンセル後・再訪時
7. 生存前衛、死亡前衛、控えの経験値差
8. C級以上のSSR/UR/EX依頼迷宮、再読込、最下層ボス、報告報酬、日次上限、広告追加
9. スキル書ドロップ、2枠/8枠入れ替え、モンスタースキル成長拒否、転生の実禁止
