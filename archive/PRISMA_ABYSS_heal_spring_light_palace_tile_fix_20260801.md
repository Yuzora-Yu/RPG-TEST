# 回復の泉重複・光の宮殿外周描画 修正メモ

## 回復の泉とイベントの重複

固定マップ／固定ダンジョンの `healSprings` を、宝箱・ボス・階段／出口・mapActors・mapActions・blockingObjects と照合した。
既存データで同一座標だったのは次の2か所。

- 魔王城ガルヴァニア 本館3階・謁見の間
  - 調査イベント「玉座の間を見渡す」: `(21,14)` を維持
  - 回復の泉: `(21,14)` から `(20,14)` へ移動
- 闇の神殿跡 2階・月影の祭壇
  - 調査イベント「祭壇の残光に触れる」: `(16,22)` を維持
  - 回復の泉: `(16,22)` から `(14,22)` へ移動

回復の泉は現在地アクションが優先されるため、同じ座標にイベントがあるとイベント表示と操作が隠れる。位置分離により双方を利用可能にした。

## 再発防止

`map_story_editor.html` の検証へ、回復の泉と次の配置の同一座標重複をエラーとして検出する処理を追加した。

- chests
- bosses
- floorLinks
- mapActors（状態別配置を含む）
- mapActions
- blockingObjects
- 別のhealSprings

## 光の宮殿の白い外周タイル

光の宮殿で使用する通行不能タイル `^` は画像なし・白単色だった。
固定マップ外側の描画では最寄りの外周タイルを繰り返すため、画面端に白い方眼状のフォールバックが広がっていた。

`^` の地形画像を `tile_light_wall` に変更し、宮殿壁のテクスチャで外周を描画するようにした。
通行不能設定とマップ形状は変更していない。

## キャッシュ

Service Workerのキャッシュ世代を `v20.20260801` へ更新した。
