# PRISMA ABYSS ギルド依頼自動生成・超越の実 実装確認報告

作成日: 2026-07-26  
対象: `RPG-TEST_guild-counter-abyss-traitbooks_cumulative_patch` を基準とした累積修正

## 1. 実装方針

今回の自動生成は、ギルド依頼に限って許可された仕様として実装した。

- 固定依頼の正本: `guild_quests.js` の `GUILD_QUEST_DATA`
- 自動生成条件の正本: `guild_quests.js` の `GUILD_QUEST_GENERATOR_MASTER`
- 掲示時に確定した依頼内容: セーブデータの `progress.guild.generatedQuests`
- ランク、掲示件数、交換品の正本: `guild_master.js`

自動生成した依頼は、受注数、報酬、対象ダンジョン、対象階層を生成時に確定してセーブする。ロードや画面再描画のたびに再抽選されない。

## 2. 通常地域の自動生成依頼

次の攻略地域を基礎テンプレートとして、名称、討伐数、ギルド経験値、ギルドポイントが変化する討伐依頼を生成する。

- イグナ火山
- 禁忌の森
- 海底神殿
- 大灯台
- ライザーク要塞
- 光の宮殿グランプリズマ
- 魔王城ガルヴァニア

掲示枠は従来どおり最大5件。受注中の依頼は更新しても維持され、空き枠だけが再抽選される。同一地域の固定依頼と自動生成依頼が同時に重複しないよう、地域単位の生成シグネチャを使用する。

掲示枠の約75%を自動生成候補として扱うが、固定の納品依頼なども残るため、掲示内容が討伐だけに偏り切らない構成とした。

## 3. 深淵の魔窟の自動生成

### 生成範囲

- 地下1階から到達済み最深階までを10階帯で候補化する。
- 最深部が10階単位でなくても、到達階までの部分帯を生成する。
  - 例: 157階到達時は `地下151～157階` が候補になる。
- 未到達階を含む依頼は生成しない。
- 冒険者ランク制限は従来の設計を維持し、「到達済み」かつ「現在ランクで受注可能」な階層帯だけを掲示候補にする。

### ランク帯

| 階層 | 必要ランク |
|---|---|
| 1～20階 | G |
| 21～40階 | F |
| 41～50階 | E |
| 51～70階 | D |
| 71～100階 | C |
| 101～140階 | B |
| 141～180階 | A |
| 181～200階 | S |

### 報酬設計

階層帯番号を `ceil(対象最深階 / 10)`、討伐数を `K` とすると、自動生成依頼の報酬は次の式で決定する。

- ギルド経験値: `20 + 階層帯番号 × 30 + K × 2`
- ギルドポイント: `5 + 階層帯番号 × 12 + K`

討伐数も深層ほど段階的に増える。乱数差を含めても、次の10階帯へ進むほど経験値・GPが必ず高くなる幅を確保した。

深淵へ到達済みで、受注可能な深淵依頼がある場合、掲示板には原則1件以上、最大2件まで深淵依頼が出る。

## 4. 討伐進捗の整合性

既存の討伐不加算問題を再発させないため、進捗判定は生成依頼でも固定依頼と同じ共通経路を使用する。

- 勝利時に、死亡済みかつ逃走していない敵を個体単位で集計する。
- 同種が複数体いれば、倒した個体数だけ加算する。
- 指定ダンジョン外では加算しない。
- 深淵依頼は指定階層帯の通常戦闘だけ加算する。
- ボス戦は `normalBattlesOnly` の依頼へ加算しない。
- 昇格試験はすべてのギルド討伐依頼から除外する。
- 受注前の討伐は遡及加算しない。
- キャンセル時はその受注回の進捗だけを破棄する。

## 5. 自動生成依頼のセーブ互換性

- `guild_quests.js` のスキーマを6へ更新した。
- 旧版の受注中依頼と進捗は維持する。
- 旧版の未受注掲示枠だけを新しい生成方式で更新する。
- 自動生成依頼は、掲示中または受注中のものだけ定義を保持する。
- 完了・キャンセル・更新で不要になった生成定義は削除し、セーブ肥大化を防ぐ。
- 自動生成依頼の累計達成数は `generatedCompletionTotal` へ集約して記録する。

## 6. 超越の実

### マスター

`items.js` にアイテムID `123` として追加した。

- 名称: 超越の実
- 種別: 育成
- 価格: 20,000 GP
- 使用対象: パーティ内のキャラクター1人
- 効果: LBを1増加
- 戦闘中使用不可
- 使用成功時に1個消費

`guild_master.js` の交換品マスターへ20,000 GPで登録した。

### LB処理

独自に数値を書き換えず、既存の正規処理 `App.addLimitBreak(character, 1, 'item')` を使用する。

- 増加元は `lbProgress.sources.item` に記録する。
- LB99では使用不可。
- 中間試練前はLB49、最終試練前はLB98の既存上限を超えない。
- 上限で使用できない場合はアイテムを消費しない。
- 成功時のみ所持数を1減らす。

## 7. 実施した確認

### 構文

次の全変更JavaScriptについて `node --check` を実施し、構文エラーがないことを確認した。

- `guild_quests.js`
- `guild.js`
- `guild_master.js`
- `items.js`
- `menus_items.js`
- `main.js`
- `menus_status.js`
- `sw.js`

### 自動生成・状態遷移

- 掲示枠が常に最大5件へ補充されること。
- 深淵依頼が1～2件に制限されること。
- 同一地域・同一階層帯の重複掲示がないこと。
- 1,000回の更新反復で掲示数と生成定義数が破綻しないこと。
- 157階到達時に151～157階の部分帯が生成可能であること。
- G～S各ランクで、受注可能な最深階層が設計表と一致すること。
- 到達階1、7、10、11、57、157、200で、候補の最深階が到達階を超えないこと。
- 深層へ進むほどEXPとGPが単調増加すること。
- 自動生成依頼の受注内容と討伐進捗がJSON保存・再読込後も維持されること。
- 正しいエリアでは個体数分加算し、異なるエリアでは加算しないこと。
- 不要な自動生成定義が掲示枠数を超えて蓄積しないこと。

### 超越の実

- アイテムID123が重複していないこと。
- 交換価格が20,000 GPであること。
- 既存LB加算経路へ `item` ソースが追加されていること。
- 試練上限時は使用不可・非消費となる分岐を確認した。

## 8. 未実施事項

大容量assetsを含む完全環境での全編通しプレイ、実ブラウザ上での長時間周回、端末別の目視確認は行っていない。今回の変更箇所については、構文、マスター参照、生成状態遷移、セーブ再読込、討伐判定を対象に点検した。
