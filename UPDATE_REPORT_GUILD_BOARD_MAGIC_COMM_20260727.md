# 依頼掲示板・ギルド移動・魔道通信 改修レポート

更新日: 2026-07-27

## 実装内容

### 1. 依頼掲示板の表示領域拡張

- 掲示板モーダルを画面上下ほぼ全域まで拡張した。
- 施設共通モーダルに画面別の幅・高さ・スクロール指定を追加し、他の施設モーダルには既定値を維持する。

### 2. 依頼更新・広告視聴を上部へ移動

- 「本日の受注」枠内に以下のボタンを配置した。
  - 依頼を更新
  - 広告視聴で受注枠 +10
- 広告追加済みの日は、同じ位置に無効状態を表示する。

### 3. スクロール対象の分離

- タイトル、説明、本日の受注状況、更新・広告ボタン、一覧見出しを固定した。
- スクロール対象をギルドクエスト一覧だけに限定した。
- 閉じるボタンもモーダル下部に固定される。

### 4. 掲示板カードへの報酬表示

各依頼カードへ、次を小さく常時表示するよう変更した。

- ギルド経験値
- ギルドポイント
- アイテム名と個数（最大2種＋残り件数）
- 装備報酬枠の有無

詳細画面の従来報酬表示も維持している。

### 5. 最低限の討伐依頼報酬

- アイテム報酬が空の討伐依頼には、依頼IDと要求ランクから決定される素材を1個保証する。
- 旧セーブ内の自動生成討伐依頼にも、再読込時に同じ補完を行う。
- 表示だけでなく、達成報告時の実際の付与処理にも同じ有効報酬を使用する。

### 6. 戦歴メニューからギルド受付へ移動

- 戦歴メニューの「ギルド」タブへ「ギルドへ移動」ボタンを追加した。
- ライザーク要塞を利用可能なセーブでは、確認後に1階の受付カウンター正面（座標 5,22）へ移動する。
- この移動ではスカイプリズムやアイテムを消費しない。
- 要塞未解放のセーブではボタンを無効化する。

### 7. 「加工」を「魔道通信」へ変更

- メインメニューの「加工」を「魔道通信」に改称した。
- 魔道通信画面から以下を利用可能にした。
  - 鍛冶屋
  - 錬金所
  - ギルド依頼掲示板（確認・更新・受注）
- 既存方針に合わせ、魔道通信の解放判定は引き続き `craftingMenu`（星詠みの触媒器 ID 111）を使用する。
- 魔道通信経由で掲示板を閉じた場合は、フィールドを経由して魔道通信画面へ戻る。

## 変更ファイル

- `facilities.js`
- `guild.js`
- `main.js`
- `menus.js`
- `menus_status.js`
- `index.html`
- `blacksmith.js`
- `sw.js`
- `docs/development-policy.md`
- `UPDATE_REPORT_GUILD_BOARD_MAGIC_COMM_20260727.md`

## キャッシュ

Service Worker のキャッシュ名を次へ更新した。

- `prisma-abyss-v3.152-guild-board-magic-comm`

## 検証について

依頼に従い、ゲーム起動およびプロジェクト付属の検証ツールは実行していない。実機での表示寸法、スクロール操作、広告ダミー処理、移動後座標の最終確認は利用側で行う。
