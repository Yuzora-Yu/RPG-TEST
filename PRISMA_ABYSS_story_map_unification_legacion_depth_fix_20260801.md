# PRISMA ABYSS ストーリー／MAP管理統合・レガシオン改修報告

作成日: 2026-08-01

## 1. 深淵以降のストーリー管理

深淵以降を別ファイルから実行時追加する構造を廃止し、地上編・深淵編・クリア後をすべて `story.js` に統合した。

- `story.js`: 会話、イベント、目的表示、進行マイルストーンの単一正本
- `story_logic.js`: イベント実行、会話再開、戦闘接続などのランタイム処理
- 旧 `abyss_story.js`: 削除

旧構成ではゲーム本体だけが `abyss_story.js` を追加読込し、MAP／ストーリーエディタは深淵イベントを読み込んでいなかった。このため、エディタで出力すると深淵イベントを欠落させる危険があった。統合後はゲームとエディタが同じ252会話スクリプト・232イベントを参照する。

## 2. 地上・深淵MAP管理

`FIXED_MAPS` と `FIXED_DUNGEON_MAPS` は維持した。町・城・単独室内と、基底設定＋複数階＋可変階を持つ固定ダンジョンではコンテナ構造が異なるため、無理に一つの配列へ平坦化すると階層継承や可変階生成が壊れやすい。

代わりに、実行・編集契約を次のように統一した。

- 共通座標配列: `floorLinks`, `mapActions`, `mapActors`, `chests`, `bosses`, `healSprings`, `floorDecorations`, `blockingObjects`
- 共通識別情報: `areaKey`, `regionKey`, `mapKind`
- 共通参照・実行: `MapRegistry`
- 欠損項目と寸法を補う `normalizeFixedMapSchema()`

深淵だけの専用MAP形式は作らず、地上側と同じ描画・衝突・アクション経路を使う。

## 3. MAP／ストーリーエディタ

`map_story_editor.html` から地上・深淵の両方を同じ手順で加工できるようにした。

### 基本情報

- `mapKind`
- `regionKey`
- 名称、テーマ、戦闘背景、寸法、入口座標

### 移動・門テンプレート

選択マスへ地形と接続データを同時配置する。

- 固定ダンジョンの上り／下り階段: `U` / `D` + `floorLinks`
- 固定ダンジョンの床移動: `S` + 自動 `floorLinks`
- 固定ダンジョンの退出: `S` + `to: "EXIT"`
- 町・城・室内間の階段／床移動: `U` / `D` / `S` + `mapActions(type: "fixedMap")`
- 町・城からの退出床: `S` + `exitPoint`
- 別固定エリアへの扉: `mapActions(type: "fixedMap")`
- 牢屋門: `blockingObjects` + 任意の隣接 `storyEvent`

牢屋門は光の宮殿と同じ `overlay_light_prison_gate_horizontal` を使用し、門上へ乗らず隣接操作する。

## 4. レガシオン内装

イベントIDと接続先を維持し、過大だった内部MAPを縮小した。

| エリア | 変更前 | 変更後 |
|---|---:|---:|
| 上層回廊 | 41×19 | 29×13 |
| 西塔 | 23×23 | 17×15 |
| 東塔 | 23×23 | 17×15 |
| 地下牢 | 31×21 | 23×15 |
| 地下神殿 | 33×23 | 21×15 |
| 謁見の間 | 35×23 | 17×13 |

謁見の間は面積比で約27%まで縮小した。上層回廊、地下神殿、謁見の間へカーペットを配置した。地下牢には4枚の物理的な牢門と、門越しの隣接イベントを配置した。

レガシオン本館は鍛冶屋、錬金屋、ギルド、闘技場、三つの内部接続、南北ワールド出口を持つため、今回は本館の機能配置を維持した。エディタから安全に縮小できる状態にはしている。

## 5. ボス討伐後の描画深度

戦後会話中のボス保持を、最前面DOMカットシーンレイヤーから通常MAP描画へ移した。

- Phaser: ボス `y × 100 + 84`
- 主人公: `y × 100 + 88`
- Canvasフォールバック: 通常MAPオブジェクト群の後、主人公の前に描画

同じ行では主人公がボスより前へ出る。上下の行ではY座標に応じて通常の前後関係になる。明示的なカットシーン演出だけは従来の専用最前面レイヤーを使う。

## 6. ギルドクエストのランダムダンジョン敵強度

依頼生成時に `power` を計算し、依頼データへ固定保存する。

```text
rankPower   = (ギルドランク添字 + 1) × 22
expPower    = floor(sqrt(累積ギルドEXP) × 4)
storyPower  = storyStep × 7
floorPower  = 深淵の実質到達階
rarityPower = SSR:20 / UR:45 / EX:85

power = max(35, floor(max(rankPower, storyPower, floorPower)
                      + expPower + rarityPower))
```

ギルドランク由来値はG/F/E/D/C/B/A/Sの順に22/44/66/88/110/132/154/176。

通常敵は依頼属性に合う技を持つ個体を優先し、元Rankが `power × 0.18`～`power × 1.35`の範囲にある個体を優先する。最大16種を依頼生成時に保存する。

通常敵倍率:

```text
SSR: 1.25 × max(1, 1 + power/400)
UR : 1.65 × max(1, 1 + power/400)
EX : 2.25 × max(1, 1 + power/400)
```

HP、MP、攻撃、防御、素早さ、魔力、魔防へ適用する。特殊条件「希少強敵50%」では通常敵倍率がさらに1.9倍。属性高位スキル、属性攻撃補正、再生、食いしばりなどが特殊条件に応じて追加される。

最下層ボス倍率:

```text
base = max(1.5,
  1 + ギルドランク添字×0.28
    + 累積ギルドEXP/850
    + storyStep×0.055
    + 深淵実質到達階/120)

boss = base × (SSR:1.25 / UR:1.75 / EX:2.5)
```

## 7. 仲間モンスターのスキル成長

- 勝利後、実際に戦闘参加した仲間モンスターが対象。
- 各対象モンスターにつき2%で成長候補を判定。
- 現在技のうち次段階が存在するものから1技を選ぶ。
- 確認後、旧技を新技へ置換する。
- 1戦で成立する成長は最大1件。
- 種別・属性が連続しない場合は成長しない。
- レベルアップだけでは新技を覚えない。
- 技枠は最大8。

### 物理

- 二段斬り → クアドリア
- 羅刹斬り → 羅刹一文字
- 刃砕き → 力脈断ち
- 道化の刃 → アルレッキーノ
- 虎爪 → レグルスの五爪

### 攻撃魔法

- イグナ → イグニア → イグナリス → イグナレクス → イグナリス・トリア
- ニヴァ → ニヴァリオ → ニヴァロア → ニヴァノヴァ → ニヴァノヴァ・デュオ
- ヴェント → ヴェンティア → ヴェントリス → ヴェントノヴァ → ヴェントノヴァ・クアドラ
- ノクス → ノクティア → ノクタリス → ノクスレクス → ノクタリス・トリア
- フラマ → フラミア → フラマリス → フラマノヴァ
- ルクス → ルクシア → ルクシオン → ルクスノヴァ → ルクシオン・ノナ
- ヴォルタ → ヴォルテリオン → ヴォルテリオン・アーク
- アステラ → アステリオン

### ブレス・回復・妨害

- 火炎の吐息 → 焔獄の息 → 灼界の業火 → イグナグラシア
- 氷霧の吐息 → 白銀凍気 → ニヴルヘイズ → ニヴァグラシア
- 闇蝕の霧 → ノクス・マレ → ノクス・アステラ
- ルナ → ルナリア → ルナリス → ルナレクス
- ルナティア → ルナリオン → ルナノヴァ
- リザ → リザリス → リザノヴァ
- ディアルマ → ディアルマス
- 毒霧 → 猛毒霧
- 獣咆 → 獣王咆 → 絶望の咆哮 → 竜王咆哮
- モルス → モルスアリア → モルスレクス
- モルスの舞 → 冥府乱舞

## 8. 点検結果

- JavaScript構文: 合格
- エディタ内スクリプト構文: 合格
- ストーリー: 252 scripts / 232 events、旧 `story.js + abyss_story.js` とキー一致
- 固定MAP: 全18件を維持。変更はレガシオン内部6MAPのみ
- 固定ダンジョン: 全26件を維持、定義差分なし
- レガシオン入口・接続・隣接操作の到達性: 合格
- MAPエディタ検証: 合格
- MAPキャラクター検証: 合格
- メインストーリー導線検証: 合格
- 固定ダンジョン退出検証: 合格
- 深淵地域検証: 451項目合格

`validate-map-safety.js` の「ABYSS F1 visual theme candidate count mismatch」は修正前ソースでも同じ状態で停止する既存不一致であり、今回の変更によるものではない。

ブラウザでの全編通しプレイは実施していない。
