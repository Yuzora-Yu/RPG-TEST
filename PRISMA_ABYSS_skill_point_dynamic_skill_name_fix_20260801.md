# スキルポイント習得画面：スキル名動的参照修正

## 変更内容

- スキルツリーの習得予定スキル名を、`desc` に保存された固定文字列ではなく、`skillId` / `skillIds` から `DB.SKILLS` を逆引きして生成するよう変更。
- スキルツリー定義の `desc` から旧スキル名称を除去し、能力補正などスキル名以外の説明だけを保持。
- `skillIds` が配列・単一数値のどちらでも動作するよう統一。
- 同一IDの重複を除去。
- 参照先が存在しない場合は `不明なスキル(ID:xxx)` と表示し、古い名称へフォールバックしない。
- HTML表示前にエスケープ処理を適用。
- Service Workerのキャッシュ世代を `v18.20260801` へ更新。

## 表示例

- `skillId: 404`
  - 修正前：`最大HP +30% / ハッスルダンス習得`
  - 修正後：`最大HP +30% / エリアヒール習得`

今後 `skills.js` / `skills_dq.js` 側でスキル名を変更すると、スキルポイント習得画面にも自動反映されます。

## 変更ファイル

- `menus_allies.js`
- `database.js`
- `sw.js`
